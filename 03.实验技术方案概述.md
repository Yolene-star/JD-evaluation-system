### 实验技术方案概述：AI驱动的岗位胜任力测评与人才画像系统

#### 一、实验背景与目标

在当前求职与招聘市场中，职位描述（JD）通常为非结构化文本，求职者难以精准评估自身能力与岗位的匹配度。本实验旨在引导学生利用大语言模型（LLM）技术，构建一个自动化的“岗位需求解析-测评框架生成-交互式测评”系统。

通过本实验，学生将掌握以下核心能力：

- **AI应用开发**：理解并实践如何调用大模型API解决具体业务问题。
- **提示词工程（Prompt Engineering）**：学习如何设计高效的提示词，引导AI完成结构化信息提取、内容生成和逻辑判断等复杂任务。
- **全栈开发流程**：体验从后端逻辑设计、API接口开发到前端交互实现的完整项目开发流程。
- **数据合规意识**：在数据获取环节，建立合法、合规使用数据的意识。

#### 二、系统总体架构设计

本系统采用轻量级的前后端分离架构，核心由三大模块构成，确保逻辑清晰，易于实现。

1. **前端交互层**：负责与用户进行交互，提供友好的操作界面。学生可根据自身技术栈选择合适的框架，核心任务是实现AI对话式聊天测评窗口以及测评报告的可视化展示。
2. **后端逻辑层**：后端逻辑层要包括JD的内容提取、精炼为能力描述；基于能力描述建立测评框架；基于测评框架完成动态人员能力达成度测评；基于能力框架和测评要求完成画像；本层负责处理所有业务逻辑。本层应合理设计业务流程、各个环节的输入输出、存储方案以及前后端的交互逻辑。
3. **AI能力层**：作为系统的“智慧大脑”，由大语言模型API提供服务。它不直接参与业务逻辑，而是根据后端传来的指令（提示词）和数据，执行文本分析、内容生成、逻辑推理等任务。并且通过对大模型API的合理规划及提示词的管理，实现对不同任务的质量优化。

#### 三、核心功能模块与实现原理

本实验的核心在于如何设计与大模型的交互逻辑。以下是三大核心功能模块的实现原理。

##### 1. 岗位JD解析与胜任力模型构建

此模块的目标是将非结构化的JD文本，转化为计算机可处理的结构化数据。

- **实现流程**：
  1. 实现对指定岗位的JD文本的收集和提炼。
  2. 实现一个精心设计的**系统提示词**驱动的流程，该流程完成从JD中提取“硬技能”、“软技能”、“经验要求”等关键信息，同时为各项能力分配权重。
  3. 后端将完整的提示词通过API发送给大模型并形成对胜任力模力的构建。
  4. 建模过程在于基于大模型能力，形成结构化的模型。
  5. 基于模型为后续生成测评框架和题目做准备。

##### 2. AI动态测评与多轮对话

此模块是整个系统的交互核心，旨在模拟真实的面试场景，实现动态追问。

- **实现流程**：
  1. **题目生成**：后端根据上一模块生成的“胜任力模型”，为每个关键能力点，通过API请求大模型生成贴合实际工作场景的面试题。
  2. **对话初始化**：后端维护一个对话历史列表，用于存储整个面试过程中的所有问答。列表的开头会包含一个系统指令，定义AI的面试官角色和行为规范。
  3. **多轮交互**：
     - 当用户提交回答后，后端将用户的回答以 `{"role": "user", "content": "..."}` 的格式添加到对话历史列表中。
     - 随后，后端将整个对话历史列表（包含系统指令、历史问答、用户最新回答）一并发送给大模型API。
     - 大模型基于完整的上下文理解对话进展，判断用户回答的质量，并决定是进行深度追问、切换下一个问题还是结束面试。
     - 后端接收到大模型的回复后，同样以 `{"role": "assistant", "content": "..."}` 的格式存入对话历史列表，并将其返回给前端展示。
  4. 通过这种方式，AI能够“记住”之前的对话内容，实现流畅、智能的多轮交互。

##### 3. 立体人才画像生成

此模块在面试结束后，对用户的综合表现进行评估，并生成可视化报告。

- **实现流程**：
  1. 当面试流程结束时，后端将完整的**对话历史记录**和最初的**岗位胜任力模型**一同打包，通过API发送给大模型。
  
  2. 此时的提示词会要求AI扮演“公正的评估官”，基于岗位要求，对候选人在对话中展现出的各项能力进行打分（如0-10分），并给出优势、短板分析及针对性的提升建议。
  
  3. 大模型返回一个包含各项分数和文本评价的JSON数据。
  
  4. 后端将此数据返回给前端。前端利用图表库（如ECharts等），将分数数据渲染成**能力雷达图**，并将文本评价和建议整合成一份完整的人才画像报告进行展示。
  
##### 4.设计更好的迭代闭环

在上述1—3的环节上，提出更好的提升框架，可以在过程中收集并完成胜任力模型、测评框架、测评过程和评价过程的不断改进策略。

#### 四、数据获取合规性要求

为保障项目的合规性与安全性，**严禁使用恶意爬虫直接抓取招聘网站数据**。本实验提供以下两种合规的数据获取方案：

1. **手动输入/文件上传**：支持用户手动将感兴趣的JD文本导入到系统中（支持不少于上百条）。
2. **浏览器插件辅助**：鼓励有能力的学生开发一个简单的浏览器扩展。当用户在招聘网站浏览岗位时，插件负责提取当前页面的结构化数据并发送至后端，而非通过爬虫主动抓取。

#### 五、实验交付物与考核标准

- **系统演示**：能够完整跑通“JD解析 → 测评框架构建 -> 交互测评 → 画像生成”的全流程。
- **源代码与文档**：提交完整的项目源码、API接口文档及项目部署说明。
- **实验报告/答辩**：重点阐述在项目中**提示词工程的设计思路**（如何引导AI输出期望结果）以及**工程化落地时遇到的难点与解决方案**（如如何处理AI输出的不稳定性、如何管理对话上下文等）。