export function AssessmentCompletionCard({ completion, completedCount, totalCount, incompleteNames = [], onEvidencePackage }: { completion: 'FULL' | 'PARTIAL'; completedCount: number; totalCount: number; incompleteNames?: string[]; onEvidencePackage?: () => void }) { return <section className="assessment-completion" aria-live="polite"><h3>{completion === 'FULL' ? '测评已完成' : '测评已部分完成'}</h3><p><strong>{completion}</strong> · 已完成 {completedCount} / {totalCount} 项能力观察。</p>{incompleteNames.length > 0 && <p>待补充：{incompleteNames.join('、')}</p>}{onEvidencePackage && <button onClick={onEvidencePackage}>查看证据包</button>}</section> }
