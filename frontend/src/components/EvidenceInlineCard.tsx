import type { EvidenceGroup } from '../types/assessment'
export function EvidenceInlineCard({ groups }: { groups: EvidenceGroup[] }) { if (!groups.length) return null; return <section className="evidence-inline" aria-label="证据摘要"><h3>回答分析</h3>{groups.map(group => <article key={group.competencyId}><header><strong>{group.competencyName}</strong><span>{group.sufficiency === 'SUFFICIENT' ? '证据充分' : group.sufficiency === 'INSUFFICIENT' ? '证据不足' : '证据不确定'}</span></header>{group.observations.map(item => <p key={item}>{item}</p>)}{group.followUpReason && <small>{group.followUpReason}</small>}</article>)}</section> }
