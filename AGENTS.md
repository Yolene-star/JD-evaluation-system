# AGENTS.md

## 1. 项目定位

本仓库用于实现“AI 驱动的岗位胜任力测评与人才画像系统”。核心闭环是：

1. 导入并解析一份或多份岗位 JD；
2. 生成有证据、有权重、可人工修订和可冻结的岗位胜任力模型；
3. 基于已确认模型开展自适应、多轮文字测评；
4. 从版本化测评证据生成可解释的能力评分、岗位匹配度和人才画像；
5. 通过测试集、Prompt 版本、调用日志和失败案例持续评估系统质量。

系统面向学生自测和教学实验。所有评分与报告都是辅助性结果，不得描述为正式招聘决策、心理测量或职业资格结论。

## 2. 开始工作前

- 先查看 `git status --short`，保护用户已有修改，不覆盖、不回滚、不顺手整理无关文件。
- 当前仓库可能仍处于设计或分阶段落地状态。先用 `rg --files` 检查实际目录，不得仅凭计划假定 `backend/`、`frontend/` 或某个接口已经存在。
- 开始某阶段前，阅读该阶段的功能规格、UI 规格和实施计划；跨阶段改动还要阅读总体路线图和全局交互基线。
- 只实现当前任务及其必要依赖。发现规格冲突、缺少跨阶段契约或需要扩大范围时，先说明影响并取得确认。
- 不得把设计稿中的示例回答、示例分数或演示数据当成真实业务结果。

## 3. 文档导航与权威顺序

核心需求与背景：

- `02.胜任力测评与人才画像系统需求.md`
- `03.实验技术方案概述.md`
- `阶段三：能力评价与人才画像.md`
- `阶段四：系统质量评估与持续优化.md`

已确认设计：

- `design/00-全局交互基线设计.md`
- `design/01-阶段一-JD解析与岗位胜任力模型设计.md`
- `docs/superpowers/specs/2026-09-09-stage1-frontend-ui-design.md`
- `docs/superpowers/specs/2026-09-09-stage2-adaptive-assessment-design.md`
- `docs/superpowers/specs/2026-09-09-stage2-frontend-ui-design.md`
- `docs/superpowers/specs/2026-09-09-stage3-capability-evaluation-profile-design.md`
- `docs/superpowers/specs/2026-09-09-stage3-report-ui-design.md`

实施入口：

- 总体顺序：`docs/superpowers/plans/2026-09-09-full-implementation-roadmap.md`
- 阶段一：`docs/superpowers/plans/2026-09-09-stage1-implementation.md`
- 阶段二：`docs/superpowers/plans/2026-09-09-stage2-adaptive-assessment-implementation.md`
- 阶段三目前有规格但尚需补充详细实施计划。

发生冲突时按以下规则处理：

1. 用户当前明确要求优先。
2. 业务范围以根目录需求文档为准；已确认规格负责细化其行为和契约。
3. 共享页面骨架、交互类型、响应式行为以全局交互基线为准；阶段规格只覆盖阶段专属内容。
4. 功能规格和 UI 规格是产品行为的事实源；实施计划描述实现顺序，不得悄悄改变规格。
5. 总体路线图控制跨阶段依赖和交付顺序，阶段计划控制阶段内任务。
6. 无法据此消解的冲突不得自行猜测，应记录具体文件与条目并询问用户。

## 4. 目标架构与技术栈

- 后端：Python 3.11+、FastAPI、Pydantic、SQLAlchemy。
- 数据库：SQLite 用于本地开发和测试，PostgreSQL 用于部署；模型和迁移必须保持兼容。
- 前端：React、TypeScript、Vite。
- 测试：pytest、Vitest、Playwright。
- AI：OpenAI 兼容接口，首接 DeepSeek；必须保留无需 API Key 也能演示核心流程的确定性解析器或 mock 适配器。
- 架构：模块化单体。除非规格和用户明确批准，不拆微服务，不引入与 MVP 无关的基础设施。

共享核心对象的名称和语义应保持稳定：`Project`、`AuditEvent`、`Evidence`、`ModelVersion`、`ConfirmedModelSnapshot`、`AssessmentEvidencePackage`、`ProfileReportVersion`。Pydantic Schema、TypeScript 类型、数据库模型和契约测试中的字段变更必须同步。

## 5. 不可破坏的业务边界

- 阶段一输出不可变的已确认岗位模型快照；后续修改必须创建新草稿或新版本。
- 阶段二只能读取 `CONFIRMED` 模型，不得修改能力项、权重或 JD 证据；输出版本化测评证据包，不计算正式分数、匹配度、雷达图或人才画像。
- 阶段三只读取 `FULL` 或 `PARTIAL` 证据包。程序负责评分、达成度、权重聚合和状态；AI 只生成受约束的解释、优势、短板与建议。
- 阶段三不得改写阶段一模型、阶段二回答、证据观察或历史报告。重新计算必须创建新报告版本。
- 阶段四应在前三阶段核心闭环稳定后实施，不得用质量中心功能阻塞 MVP。
- 阶段转换、权限、权重、评分、幂等和版本冻结由确定性程序控制，不能交给 LLM 或仅靠前端按钮隐藏。
- 工作台操作和对话命令必须作用于同一份领域数据，并写入统一审计事件。
- 所有正式能力项、评分和报告结论必须能回溯到原始 JD 或测评回答；缺少证据时标记待补充、不确定或未评价，不得推测补全。

## 6. AI 与 Prompt 工程规范

- 将 LLM 放在适配层之后；业务服务不得依赖某一家模型供应商的私有响应格式。
- 优先使用结构化输出和明确 Schema。模型结果在持久化或推进状态前必须完成枚举、范围、引用、证据片段和必填字段校验。
- AI 不得直接写数据库、推进状态机、创建正式能力项、修改权重或计算最终评分。
- Prompt 输入遵循最小上下文原则，只传入完成当前任务所需的版本化事实和证据。
- 保留原始输入与模型输出，记录 `timestamp`、模型、Prompt 版本、延迟、token 用量和错误；敏感信息应脱敏，密钥绝不进入日志或仓库。
- Prompt 采用显式版本管理。修改 Prompt 时说明原因，补充代表性成功、边界和失败案例，并运行回归评估。
- 对超时、限流、无效 JSON、Schema 不匹配和证据引用失败使用明确的可重试错误。失败不能清除用户原回答，也不能造成重复轮次或部分状态提交。

## 7. 前端与交互约束

- 阶段一至三复用同一套 `AppShell`、阶段轨道、历史任务导航、对话时间线、`StageWorkbench` 和 `EvidenceDrawer`，不得各自复制一套页面外壳。
- 桌面端采用折叠导航、主对话区和右侧工作台；窄屏把导航和工作台变为可访问的侧滑面板，核心内容不得出现横向滚动。
- 后端快照是状态事实源。前端不得自行推断或提前推进业务状态，变更请求完成后用服务端响应替换本地快照。
- 视觉保持温和专业：暖白背景、低饱和青绿色、清晰层级；禁止紫色渐变、过度装饰和把实验评分包装成权威仪表盘。
- 交互不能只靠颜色表达状态。保证键盘操作、可见焦点、语义标签、状态播报、至少 44px 触控目标和 `prefers-reduced-motion` 支持。
- 确定性状态可使用固定 UI 文案；Agent 的业务分析必须来自现场数据，禁止通过关键词返回预写完整回答。

## 8. 数据合规与安全

- 禁止编写或接入恶意招聘网站爬虫。JD 来源仅限用户手动输入、脱敏文件上传、用户主动操作的浏览器扩展或明确合规的数据源。
- 文件上传要校验类型、大小和内容；保留原文与清洗结果，不执行上传内容中的代码或指令。
- LLM 输出、JD 文本、简历和用户回答均视为不可信输入；防范提示注入、越权引用、HTML 注入和路径注入。
- 密钥通过环境变量或本地密钥设施注入。只提交 `.env.example`，不得提交真实密钥、token、个人信息、真实测评记录或生产数据库。
- API 必须在后端校验资源归属和状态前置条件；前端校验只用于改善体验。

## 9. 实现工作流

1. 明确当前任务对应的需求、规格、实施计划和验收条件。
2. 检查现有实现与测试，沿用已有命名、目录和模式。
3. 先写一个能证明目标行为的失败测试，并确认失败原因正确。
4. 做满足测试的最小实现，不夹带无关重构或未来阶段功能。
5. 运行最窄相关测试，再运行受影响层级的测试、构建和静态检查。
6. 同步更新 API Schema、TypeScript 类型、迁移、文档和追踪关系。
7. 检查 `git diff` 与 `git status`，确认只改了预期文件，没有覆盖用户工作。

执行仓库内已批准的实施计划时，遵循其任务顺序、测试和提交检查点。没有明确计划或用户要求时，不自行提交、推送、创建分支或改写历史。

## 10. 验证命令

根据仓库当前实际状态选择命令；目录或配置尚未创建时，先按对应实施计划建立，不伪造通过结果。

```powershell
python -m pytest backend/tests -q
npm --prefix frontend run test
npm --prefix frontend run test:e2e
npm --prefix frontend run build
git diff --check
```

开发中先运行针对性测试，例如单个 pytest 文件或单个 Vitest 测试文件。涉及前后端契约时，两侧测试都要运行；涉及响应式、恢复流程或跨阶段闭环时必须运行 Playwright。任何“已完成”“已修复”或“测试通过”的结论都必须附有本次实际执行结果。

## 11. 完成定义

任务只有在以下条件满足后才算完成：

- 行为符合相关需求、全局基线和阶段规格，且没有越过阶段边界；
- 正常、边界、失败恢复与关键不变量有自动化测试；
- AI 输出受 Schema 和业务规则约束，正式结论具有可追溯证据；
- 前端在桌面和窄屏可用，并满足基本无障碍要求；
- 无密钥、个人数据、生成物或调试日志误入版本控制；
- 相关测试、构建和 `git diff --check` 已实际通过；
- 文档与实际命令、接口和环境变量保持一致；
- 最终说明列出改动文件、验证结果和仍存在的限制。
