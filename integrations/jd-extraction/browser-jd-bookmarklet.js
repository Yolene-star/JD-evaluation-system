// Replace PROJECT_ID with the active task id, then save as a browser bookmark.
javascript:(async()=>{const text=(s)=>{const e=document.querySelector(s);return e?.innerText?.trim()||''};const title=text('h1')||document.title||'网页岗位';const description=text('[class*="job-description"]')||text('[class*="job-detail"]')||document.body.innerText.trim();const projectId=prompt('当前任务 ID');if(!projectId)return;const r=await fetch(`http://127.0.0.1:8001/api/projects/${projectId}/jds/browser`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({page_title:document.title,url:location.href,extracted:{job_title:title,description}})});alert(r.ok?'JD 已发送到当前任务':'提取失败：'+await r.text())})()
